# CFR-071: command_result schema versionが文字列直書き

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | maintainability |
| Area | Automation schema |
| Source | `crates/codefire-cli/src/automation.rs:14` |

## Problem

`schema: "codefire.command_result.v1"` が関数内に直接書かれている。

## Why It Matters

schema version移行時にcompatibility判断やdocs連携が弱い。

## Failure Mode / Review Hypothesis

機能追加時に同じ仕様を複数箇所へ反映する必要があり、修正漏れや不変条件破壊につながる。 schema version移行時にcompatibility判断やdocs連携が弱い。

## Code Evidence

- Primary source: `crates/codefire-cli/src/automation.rs:14`
- Observed implementation fact: `schema: "codefire.command_result.v1"` が関数内に直接書かれている。

## Recommended Fix

schema constantsとschema changelogを導入する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 動作変更なしの分割または定数化から始める。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: none identified in this review batch.

## Acceptance Criteria

- schema IDが定数化される。
- docs/automation_interface.mdとテストが同じIDを参照する。

## Test Plan

- automation schema id test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
