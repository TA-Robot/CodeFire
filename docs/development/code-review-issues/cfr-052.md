# CFR-052: doctorがactive stateのJSON shapeを検証していない

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | correctness-risk |
| Area | Doctor |
| Source | `crates/codefire-cli/src/doctor/checks.rs:226` |

## Problem

active state filesはJSONとして読めるかだけ確認し、schemaや必須fieldは見ない。

## Why It Matters

壊れた `fires.json` や `state.json` がJSONとしてvalidなら見逃す可能性がある。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件・並行実行・破損状態でCodeFireの整合性判断がずれる可能性がある。 壊れた `fires.json` や `state.json` がJSONとしてvalidなら見逃す可能性がある。

## Code Evidence

- Primary source: `crates/codefire-cli/src/doctor/checks.rs:226`
- Observed implementation fact: active state filesはJSONとして読めるかだけ確認し、schemaや必須fieldは見ない。

## Recommended Fix

active state用の型/schema validationを追加する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 最小fixtureで現状の境界挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-050](cfr-050.md), [CFR-051](cfr-051.md), [CFR-053](cfr-053.md), [CFR-054](cfr-054.md)

## Acceptance Criteria

- fires/resolutions/verification/stateの必須field欠落をdoctorが検出する。
- 破損時に修復/再scan next_actionを出す。

## Test Plan

- invalid active state shape doctor test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
