# CFR-057: storage next_actionが `codefire-rs` 固定

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | ux |
| Area | Storage report |
| Source | `crates/codefire-cli/src/storage.rs:205` |

## Problem

storage warningのnext action commandが `codefire-rs storage report --json` になっている。

## Why It Matters

install後binaryが `codefire` の環境でAI toolが存在しないcommandを提示される。

## Failure Mode / Review Hypothesis

人間やAI toolがログ/JSONを誤解し、次の操作を誤る可能性がある。 install後binaryが `codefire` の環境でAI toolが存在しないcommandを提示される。

## Code Evidence

- Primary source: `crates/codefire-cli/src/storage.rs:205`
- Observed implementation fact: storage warningのnext action commandが `codefire-rs storage report --json` になっている。

## Recommended Fix

binary name定数化issueと合わせ、全next_actionを配布名に統一する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: text/JSON/next_actionsの期待出力をsnapshotで固定する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-055](cfr-055.md), [CFR-056](cfr-056.md)

## Acceptance Criteria

- storage next_actionがhelpのbinary名と一致する。
- snapshot testで固定する。

## Test Plan

- storage next_action command test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
