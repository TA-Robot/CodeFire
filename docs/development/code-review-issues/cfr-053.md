# CFR-053: opened registryのopen path整合性をdoctorが検証していない

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | correctness-risk |
| Area | Doctor |
| Source | `crates/codefire-cli/src/doctor/checks.rs:166` |

## Problem

opened registryはbase commitとactive_state_pathの存在を見るが、`.codefire-open` markerやopen pathとの相互整合性は見ない。

## Why It Matters

移動・コピー・手動編集されたopen directoryの不整合をdoctorで検出しづらい。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件・並行実行・破損状態でCodeFireの整合性判断がずれる可能性がある。 移動・コピー・手動編集されたopen directoryの不整合をdoctorで検出しづらい。

## Code Evidence

- Primary source: `crates/codefire-cli/src/doctor/checks.rs:166`
- Observed implementation fact: opened registryはbase commitとactive_state_pathの存在を見るが、`.codefire-open` markerやopen pathとの相互整合性は見ない。

## Recommended Fix

registry open.path、marker、active_state_path、branch名を相互検証する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 最小fixtureで現状の境界挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-050](cfr-050.md), [CFR-051](cfr-051.md), [CFR-052](cfr-052.md), [CFR-054](cfr-054.md)

## Acceptance Criteria

- marker不一致をdoctorが検出する。
- 存在しないopen pathをwarning/errorに分類する。

## Test Plan

- opened registry marker consistency test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
