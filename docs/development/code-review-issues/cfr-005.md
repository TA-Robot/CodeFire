# CFR-005: clean scanの空状態表示が曖昧

## Summary

| Field | Value |
|---|---|
| Status | fixed |
| Severity | medium |
| Type | ux |
| Area | Scan output |
| Source | `crates/codefire-cli/src/main.rs:1213` |

## Problem

`print_scan` は常に `Changed atoms:` と `Open fires:` を出し、空の場合の `none` 表示を持たない。

## Why It Matters

AI toolや人間がログだけを見たとき、正常な空状態か表示欠落か判断しにくい。

## Failure Mode / Review Hypothesis

人間とAI toolのログ解釈がずれ、次に実行すべき操作を誤る可能性がある。AI toolや人間がログだけを見たとき、正常な空状態か表示欠落か判断しにくい。

## Code Evidence

- Primary source: `crates/codefire-cli/src/main.rs:1213`
- Observed implementation fact: `print_scan` は常に `Changed atoms:` と `Open fires:` を出し、空の場合の `none` 表示を持たない。

## Recommended Fix

空の場合は `Changed atoms: none` と `Open fires: none` を明示する。

## Implementation Notes

- Priority: 機能追加の前後で一緒に片付ける。単独修正でも価値はあるが、関連領域の改修時にまとめると効率がよい。
- First step: 標準出力、JSON、next_actionsの3面で同じ意味を返すようにする。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: none identified in this review batch.

## Acceptance Criteria

- clean scanの標準出力に `none` が出る。
- JSON outputは空配列のまま維持する。

## Test Plan

- clean scan snapshot/golden test。

## Triage Checklist

- [x] 再現fixtureまたは現状固定テストを追加した
- [x] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [x] `docs/development/history.md` または関連仕様を更新した
- [x] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した

## v0.7 Resolution

`render_scan` を追加し、clean scanでは `Changed atoms: none` と `Open fires: none` を明示するようにした。確認テストは `scan_and_verify_text_outputs_include_empty_summaries`。
