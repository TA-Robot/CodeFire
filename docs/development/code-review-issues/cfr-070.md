# CFR-070: idempotency resultにplanを保存して再利用している

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | correctness-risk |
| Area | Remote idempotency |
| Source | `crates/codefire-cli/src/remote/idempotency.rs:141` |

## Problem

replay時に保存済み `plan` をそのまま返す。

## Why It Matters

環境やremote状態が変わっても古いnext_actions/open_dir/pathを返す可能性がある。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件・並行実行・破損状態でCodeFireの整合性判断がずれる可能性がある。 環境やremote状態が変わっても古いnext_actions/open_dir/pathを返す可能性がある。

## Code Evidence

- Primary source: `crates/codefire-cli/src/remote/idempotency.rs:141`
- Observed implementation fact: replay時に保存済み `plan` をそのまま返す。

## Recommended Fix

replay resultと現在context由来のnext_actionsを分ける。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 最小fixtureで現状の境界挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-067](cfr-067.md), [CFR-068](cfr-068.md), [CFR-069](cfr-069.md)

## Acceptance Criteria

- replayed responseに `replayed: true` が入る。
- 古いplan pathを現在操作の事実として扱わない。

## Test Plan

- remote idempotency replay plan test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
