# CFR-021: Markdown Atom抽出がコードフェンスを考慮しない

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | correctness-risk |
| Area | Markdown parser |
| Source | `crates/codefire-core/src/lib.rs:1084` |

## Problem

heading抽出は全行の `# ` を見るだけで、fenced code block内かどうかを追跡しない。

## Why It Matters

Markdown内サンプルコードの `# REQ-...` がAtomとして誤検出される可能性がある。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件や並行実行でCodeFireの整合性表示・保存結果がずれる可能性がある。Markdown内サンプルコードの `# REQ-...` がAtomとして誤検出される可能性がある。

## Code Evidence

- Primary source: `crates/codefire-core/src/lib.rs:1084`
- Observed implementation fact: heading抽出は全行の `# ` を見るだけで、fenced code block内かどうかを追跡しない。

## Recommended Fix

Markdown parserを導入するか、少なくともfenced code block状態を追跡する。

## Implementation Notes

- Priority: 機能追加の前後で一緒に片付ける。単独修正でも価値はあるが、関連領域の改修時にまとめると効率がよい。
- First step: 小さな再現fixtureを作って、現状の挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: none identified in this review batch.

## Acceptance Criteria

- code fence内のheading風テキストはAtomにならない。
- 通常headingは従来通り抽出される。

## Test Plan

- fenced heading fixture test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
