# CFR-067: remote idempotency keyが空文字をOptionとして受ける

## Summary

| Field | Value |
|---|---|
| Status | fixed |
| Severity | medium |
| Type | correctness-risk |
| Area | Remote idempotency |
| Source | `crates/codefire-cli/src/remote/idempotency.rs:12` |

## Problem

`remote_idempotency_key` は空文字をfilterせず `Some("")` にする。

## Why It Matters

後段でrejectされるとしても、request payload解釈とdiagnosticが分かりにくい。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件・並行実行・破損状態でCodeFireの整合性判断がずれる可能性がある。 後段でrejectされるとしても、request payload解釈とdiagnosticが分かりにくい。

## Code Evidence

- Primary source: `crates/codefire-cli/src/remote/idempotency.rs:12`
- Observed implementation fact: `remote_idempotency_key` は空文字をfilterせず `Some("")` にする。

## Recommended Fix

空文字はNoneまたはusage errorとして早期に扱う。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 最小fixtureで現状の境界挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-068](cfr-068.md), [CFR-069](cfr-069.md), [CFR-070](cfr-070.md)

## Acceptance Criteria

- 空文字keyのHTTP/local remote挙動が一致する。
- diagnosticが明確になる。

## Test Plan

- empty remote idempotency key test。

## Triage Checklist

- [x] 再現fixtureまたは現状固定テストを追加した
- [x] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [x] `docs/development/history.md` または関連仕様を更新した
- [x] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した

## v0.7 Resolution

remote request payloadの空 `idempotency_key` は `None` として扱うようにした。確認テストは `remote_idempotency_key_ignores_empty_string`。
