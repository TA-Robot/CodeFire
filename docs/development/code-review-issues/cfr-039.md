# CFR-039: integration testが巨大単一ファイルに集中している

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | test-gap |
| Area | Tests |
| Source | `crates/codefire-cli/src/tests.rs:1`, `tests/test_codefire_cli.py:1` |

## Problem

Rust CLI testsは約4,401行、Python CLI testsは約3,462行の大きな単一ファイルになっている。

## Why It Matters

目的別のテスト発見性が低く、修正時に必要なテスト範囲を選びにくい。

## Failure Mode / Review Hypothesis

目的別のテスト発見性が低く、修正時に必要なテスト範囲を選びにくい。

## Code Evidence

- Primary source: `crates/codefire-cli/src/tests.rs:1`, `tests/test_codefire_cli.py:1`
- Observed implementation fact: Rust CLI testsは約4,401行、Python CLI testsは約3,462行の大きな単一ファイルになっている。

## Recommended Fix

`tests/status.rs`, `tests/remote.rs`, `tests/diff.rs` などdomain別に分割し、fixture helperを共有する。

## Implementation Notes

- Priority: 機能追加の前後で一緒に片付ける。単独修正でも価値はあるが、関連領域の改修時にまとめると効率がよい。
- First step: テストの発見性と失敗時の原因特定を優先して分割する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: none identified in this review batch.

## Acceptance Criteria

- 新規テストはdomain別ファイルへ追加される。
- 既存巨大テストは段階的に分割される。

## Test Plan

- 分割後も `cargo test --workspace` が通る。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
