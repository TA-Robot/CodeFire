# CFR-080: Python fallback専用commandの移行状態がコードから検証されない

## Summary

| Field | Value |
|---|---|
| Status | fixed |
| Severity | medium |
| Type | maintainability |
| Area | Release docs |
| Source | `docs/development/known-limitations.md:65` |

## Problem

known limitationsにはPython fallback専用commandが列挙されているが、Rust help/installerとの自動整合チェックはない。

## Why It Matters

Rust移行が進んでもdocsが古くなり、ユーザーがfallback要否を誤解する。

## Failure Mode / Review Hypothesis

機能追加時に同じ仕様を複数箇所へ反映する必要があり、修正漏れや不変条件破壊につながる。 Rust移行が進んでもdocsが古くなり、ユーザーがfallback要否を誤解する。

## Code Evidence

- Primary source: `docs/development/known-limitations.md:65`
- Observed implementation fact: known limitationsにはPython fallback専用commandが列挙されているが、Rust help/installerとの自動整合チェックはない。

## Recommended Fix

help outputとknown limitationsのコマンド一覧を比較するdocs testを追加する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 動作変更なしの分割または定数化から始める。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: none identified in this review batch.

## Acceptance Criteria

- Rust helpにあるcommandとfallback一覧の矛盾を検出する。
- Python fallback削除条件が文書化される。
- `known-limitations.md` のfallback command listとRust `COMPLETION_COMMANDS` / help command listの不整合をテストで検出する。

## Test Plan

- `known_limitations_python_fallback_commands_stay_out_of_rust_cli`。

## Triage Checklist

- [x] 再現fixtureまたは現状固定テストを追加した
- [x] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [x] `docs/development/history.md` または関連仕様を更新した
- [x] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
