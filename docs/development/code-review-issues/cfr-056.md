# CFR-056: storage reportのobject type判定がrecord wrapperを考慮していない可能性がある

## Summary

| Field | Value |
|---|---|
| Status | fixed |
| Severity | medium |
| Type | correctness-risk |
| Area | Storage report |
| Source | `crates/codefire-cli/src/storage.rs:330` |

## Problem

object fileは `ObjectRecord { object_id, type, hash, payload }` だが、reportはtop-level `type` を読んでいる。

## Why It Matters

payload typeとrecord typeのどちらを集計しているか曖昧になり、unknown扱いや誤集計につながる可能性がある。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件・並行実行・破損状態でCodeFireの整合性判断がずれる可能性がある。 payload typeとrecord typeのどちらを集計しているか曖昧になり、unknown扱いや誤集計につながる可能性がある。

## Code Evidence

- Primary source: `crates/codefire-cli/src/storage.rs:330`
- Observed implementation fact: object fileは `ObjectRecord { object_id, type, hash, payload }` だが、reportはtop-level `type` を読んでいる。

## Recommended Fix

ObjectRecordとしてparseし、record.type_tagとpayload.typeの整合性も診断する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 最小fixtureで現状の境界挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-055](cfr-055.md), [CFR-057](cfr-057.md)

## Acceptance Criteria

- record wrapperのtypeで集計される。
- payload type mismatchをwarningにする。

## Test Plan

- storage report object record type test。

## Resolution

- Fixed in v0.7: full `storage report` now parses files as `ObjectRecord` wrappers and groups by `record.type`, not by an ambiguous top-level payload type.
- If `payload.type` exists and differs from `record.type`, storage report emits a `payload_type_mismatch` warning while continuing the report.
- Tests now tamper a blob payload type and assert the record is still counted as `blob` while the mismatch warning is reported.

## Triage Checklist

- [x] 再現fixtureまたは現状固定テストを追加した
- [x] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [x] `docs/development/history.md` または関連仕様を更新した
- [x] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
