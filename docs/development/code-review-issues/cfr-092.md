# CFR-092: branch subcommandのunsupported pathがstructured errorにならない

## Summary

| Field | Value |
|---|---|
| Status | fixed |
| Severity | medium |
| Type | ux |
| Area | Branch CLI |
| Source | `crates/codefire-cli/src/main.rs:470` |

## Problem

`branch` のunsupported subcommandは、以前はplain usage errorへ落ちるか、JSON指定時にも汎用error envelopeから不正なhelp next_actionを生成し得た。

## Why It Matters

branch管理はCodeFireの基本操作であり、AI toolはbranch一覧、branch詳細、open状態を問い合わせたい。unsupported commandでもmachine-readableに「未対応」「代替はbranch list」と返るべきである。

## Failure Mode / Review Hypothesis

`branch show --json` がtext errorを返し、automationがJSON parseに失敗する。helpにもbranchの能力が十分表れず、tool側のcapability discoveryが不安定になる。

## Code Evidence

- Primary source: `crates/codefire-cli/src/main.rs:470`
- Observed implementation fact: branch command dispatch handles `list` and otherwise returns usage-style error.
- Related source: `crates/codefire-cli/src/automation.rs:9`
- Observed implementation fact: common envelope exists but unsupported branch path does not use it.

## Recommended Fix

branch unsupported subcommand errorを専用JSON envelopeに通し、supported subcommandsと実行可能なnext_actionsを返す。

## Fix

`branch <unknown> --json` now returns:

- `data.type = codefire_branch_error`
- `data.unsupported_subcommand`
- `data.supported_subcommands = ["list", "show"]`
- diagnostic kind `unsupported_subcommand`
- next actions for `codefire branch --help`, `codefire branch list --json`, and `codefire branch show --json`

## Implementation Notes

- Priority: medium。
- First step: `branch show --json` の現状固定test。
- Boundary: branch show実装は別issueでもよいが、error shapeは共通化する。
- Related issues: CFB-058, CFR-081.

## Acceptance Criteria

- [x] unsupported branch subcommandがJSON envelope errorを返す。
- [x] diagnosticにsupported subcommandsと代替next actionがある。
- [x] branch command helpが実装済みsubcommandと一致する。

## Test Plan

- `cargo test -p codefire-cli branch_list_reads_repo_from_open_marker_and_validates_heads`
- `cargo clippy -p codefire-cli --all-targets -- -D warnings`

## Triage Checklist

- [x] 再現fixtureまたは現状固定テストを追加した
- [x] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [x] `docs/development/history.md` または関連仕様を更新した
- [x] 必要な確認コマンドを実行した
