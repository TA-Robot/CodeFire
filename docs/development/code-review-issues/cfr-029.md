# CFR-029: common ancestor計算が履歴全探索寄り

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | correctness-risk |
| Area | Merge |
| Source | `crates/codefire-cli/src/main.rs:4043` |

## Problem

左右それぞれの全ancestor distance mapを作ってから共通祖先を選ぶ。

## Why It Matters

履歴が深くなるとmerge/remote ancestor checkが遅くなり、メモリ使用も増える。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件や並行実行でCodeFireの整合性表示・保存結果がずれる可能性がある。履歴が深くなるとmerge/remote ancestor checkが遅くなり、メモリ使用も増える。

## Code Evidence

- Primary source: `crates/codefire-cli/src/main.rs:4043`
- Observed implementation fact: 左右それぞれの全ancestor distance mapを作ってから共通祖先を選ぶ。

## Recommended Fix

bidirectional BFSやgeneration numberを使い、探索範囲を減らす。

## Implementation Notes

- Priority: 機能追加の前後で一緒に片付ける。単独修正でも価値はあるが、関連領域の改修時にまとめると効率がよい。
- First step: 小さな再現fixtureを作って、現状の挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: none identified in this review batch.

## Acceptance Criteria

- generation metadataがある場合はそれを使う。
- 深い履歴fixtureで処理時間が線形以下に抑えられる。

## Test Plan

- deep commit graph test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
