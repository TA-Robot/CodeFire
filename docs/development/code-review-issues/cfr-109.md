# CFR-109: evidence commandの非ゼロ終了が成功証跡のように見える

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | high |
| Type | correctness-risk |
| Area | Evidence capture |
| Source | `crates/codefire-cli/src/evidence.rs:579`, `crates/codefire-cli/src/evidence.rs:639` |

## Problem

command captureはcommand exit codeをmetadataに保存するが、非ゼロ終了をどの程度success/failureとして扱うかのpolicyが弱い。証跡objectの作成自体は成功するため、失敗したtest commandが「成功証跡」と誤読され得る。

## Why It Matters

fire解消のevidenceとして失敗commandを添付できると、整合性保証が弱くなる。もちろん失敗ログ自体も証跡になり得るが、その場合は明示的に `allow_failed_command` のようなintentが必要である。

## Failure Mode / Review Hypothesis

`python -m unittest` が失敗してもevidence objectは作られ、resolution evidence refとして使えてしまう。verifyがevidence command exit codeの意味を再評価しない場合、失敗テストでfireを消せる。

## Code Evidence

- Primary source: `crates/codefire-cli/src/evidence.rs:579`
- Observed implementation fact: shell command capture records process output.
- Related source: `crates/codefire-cli/src/evidence.rs:639`
- Observed implementation fact: finish step records command metadata but policy gateが必要。

## Recommended Fix

デフォルトでは非ゼロexitをevidence add failureまたはblocking diagnosticにする。失敗ログを証跡化したい場合は `--allow-failed-command` を要求し、evidence payloadに `proof_status=failed-command-captured` を明示する。

## Implementation Notes

- Priority: high。evidence-backed resolutionの信頼性に関わる。
- First step: nonzero command captureの現状固定testを追加する。
- Boundary: command実行管理機能ではなく、証跡意味論の保護。
- Related issues: CFR-041 fixed後の残課題。

## Acceptance Criteria

- defaultでは非ゼロcommandを成功証跡として保存しない。
- 明示allow時はwarning diagnosticとmetadataが出る。
- resolution/verify側がfailed-command evidenceを見分けられる。

## Test Plan

- failing command evidence addが失敗するtest。
- `--allow-failed-command` で保存されるがwarningが出るtest。
- failed-command evidenceをresolutionに使った場合のverify policy test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] 必要な確認コマンドを実行した
