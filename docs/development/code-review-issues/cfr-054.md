# CFR-054: doctor issue severityがerror/warningの2段階に閉じている

## Summary

| Field | Value |
|---|---|
| Status | fixed |
| Severity | medium |
| Type | maintainability |
| Area | Doctor |
| Source | `crates/codefire-cli/src/doctor/report.rs:1` |

## Problem

doctor outputはissue severityをerror/warning中心に扱い、repairable/blocking/security等の分類がない。

## Why It Matters

AI toolが自動修復可否や優先度を判断しにくい。

## Failure Mode / Review Hypothesis

機能追加時に同じ仕様を複数箇所へ反映する必要があり、修正漏れや不変条件破壊につながる。 AI toolが自動修復可否や優先度を判断しにくい。

## Code Evidence

- Primary source: `crates/codefire-cli/src/doctor/report.rs:1`
- Observed implementation fact: doctor outputはissue severityをerror/warning中心に扱い、repairable/blocking/security等の分類がない。

## Recommended Fix

doctor diagnosticにcategory、repairability、blockingを追加する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 動作変更なしの分割または定数化から始める。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-050](cfr-050.md), [CFR-051](cfr-051.md), [CFR-052](cfr-052.md), [CFR-053](cfr-053.md)

## Acceptance Criteria

- JSON diagnosticsにcategoryとrepairableが入る。
- text outputもblocking件数を出す。

## Test Plan

- doctor diagnostic schema test。

## Resolution

- Fixed in v0.7: doctor diagnostics now include `category`, `repairable`, and `blocking` fields in both `data.issues` and top-level command diagnostics.
- Doctor JSON includes `issue_counts.total` and `issue_counts.blocking`; text output prints `blocking issues: <n>` and includes category/repairable/blocking beside each issue.
- Existing severity remains `error` / `warning` for compatibility, while automation can use the new fields for repair routing and priority.

## Triage Checklist

- [x] 再現fixtureまたは現状固定テストを追加した
- [x] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [x] `docs/development/history.md` または関連仕様を更新した
- [x] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
