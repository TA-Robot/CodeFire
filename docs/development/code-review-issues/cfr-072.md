# CFR-072: next_actionsが多すぎる場合の上限がない

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | ux |
| Area | Automation next_actions |
| Source | `crates/codefire-cli/src/automation.rs:127` |

## Problem

scan open_firesごとにcontext/extinguish actionを2件ずつ追加する。

## Why It Matters

fireが多いとJSONが大きくなり、AI toolが重要actionを見落としやすい。

## Failure Mode / Review Hypothesis

人間やAI toolがログ/JSONを誤解し、次の操作を誤る可能性がある。 fireが多いとJSONが大きくなり、AI toolが重要actionを見落としやすい。

## Code Evidence

- Primary source: `crates/codefire-cli/src/automation.rs:127`
- Observed implementation fact: scan open_firesごとにcontext/extinguish actionを2件ずつ追加する。

## Recommended Fix

next_actionsにlimit/summary/groupingを追加する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: text/JSON/next_actionsの期待出力をsnapshotで固定する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: none identified in this review batch.

## Acceptance Criteria

- open fire多数時はgrouped actionになる。
- 省略件数がJSONに出る。

## Test Plan

- scan next_actions limit test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
