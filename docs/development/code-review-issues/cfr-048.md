# CFR-048: link batch appendが既存YAML構造を保てない可能性がある

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | correctness-risk |
| Area | Link batch |
| Source | `crates/codefire-cli/src/link_batch.rs:260` |

## Problem

既存textに `links:` がなければ末尾へ追加し、既存構造のparse/roundtripを行わない。

## Why It Matters

コメント、別section、インデント付き構造があるファイルで意図しない場所にlinksを追加する可能性がある。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件・並行実行・破損状態でCodeFireの整合性判断がずれる可能性がある。 コメント、別section、インデント付き構造があるファイルで意図しない場所にlinksを追加する可能性がある。

## Code Evidence

- Primary source: `crates/codefire-cli/src/link_batch.rs:260`
- Observed implementation fact: 既存textに `links:` がなければ末尾へ追加し、既存構造のparse/roundtripを行わない。

## Recommended Fix

link fileを構造としてparseし、書き戻し方針をADR化する。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 最小fixtureで現状の境界挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-047](cfr-047.md), [CFR-049](cfr-049.md)

## Acceptance Criteria

- 既存links sectionへ正しく追記する。
- unsupported構造では明示errorになる。

## Test Plan

- link batch existing YAML fixture tests。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
