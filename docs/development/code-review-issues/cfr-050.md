# CFR-050: doctorのrepo layout必須dir一覧が古い

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | correctness-risk |
| Area | Doctor |
| Source | `crates/codefire-cli/src/doctor/checks.rs:7` |

## Problem

`REQUIRED_REPO_DIRS` は `objects`, `branches`, `opened`, `active` だけで、locks/remotes/cache/idempotency等を必須確認しない。

## Why It Matters

v0.6で増えた保存領域の欠落をdoctorが見逃す可能性がある。

## Failure Mode / Review Hypothesis

通常ケースでは動いても、境界条件・並行実行・破損状態でCodeFireの整合性判断がずれる可能性がある。 v0.6で増えた保存領域の欠落をdoctorが見逃す可能性がある。

## Code Evidence

- Primary source: `crates/codefire-cli/src/doctor/checks.rs:7`
- Observed implementation fact: `REQUIRED_REPO_DIRS` は `objects`, `branches`, `opened`, `active` だけで、locks/remotes/cache/idempotency等を必須確認しない。

## Recommended Fix

repo layout定義をstore/layout moduleの単一sourceに寄せ、doctorもそれを使う。

## Implementation Notes

- Priority: 関連領域を触るタイミングで一緒に片付ける。単独修正でも価値はある。
- First step: 最小fixtureで現状の境界挙動を固定してから修正する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-051](cfr-051.md), [CFR-052](cfr-052.md), [CFR-053](cfr-053.md), [CFR-054](cfr-054.md)

## Acceptance Criteria

- v0.6で作る全必須dirをdoctorが確認する。
- missing locks/remotes/idempotencyを検出する。

## Test Plan

- doctor layout completeness test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
