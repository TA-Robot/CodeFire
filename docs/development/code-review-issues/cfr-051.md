# CFR-051: doctorがobject JSONを全件parseする

## Summary

| Field | Value |
|---|---|
| Status | open |
| Severity | medium |
| Type | performance |
| Area | Doctor |
| Source | `crates/codefire-cli/src/doctor/checks.rs:63` |

## Problem

`check_object_records` はobjects配下のJSONを全件read/parse/validateする。

## Why It Matters

大規模object storeでdoctorが重くなり、日常的なhealth checkに使いづらくなる。

## Failure Mode / Review Hypothesis

入力規模が増えると処理時間、I/O、メモリ使用量が急増する可能性がある。 大規模object storeでdoctorが重くなり、日常的なhealth checkに使いづらくなる。

## Code Evidence

- Primary source: `crates/codefire-cli/src/doctor/checks.rs:63`
- Observed implementation fact: `check_object_records` はobjects配下のJSONを全件read/parse/validateする。

## Recommended Fix

quick/full mode、sample mode、mtime cacheを導入する。

## Implementation Notes

- Priority: 大規模repoや実験artifactが増える前に上限設計を入れる。
- First step: 最悪ケースのサイズを決め、修正前後の計算量と実測を比較する。
- Boundary: AI agent運用機能やmarketplaceをCodeFire本体へ入れず、整合性管理・履歴封印・自動化tool向けI/Oの改善として扱う。
- Related issues: [CFR-050](cfr-050.md), [CFR-052](cfr-052.md), [CFR-053](cfr-053.md), [CFR-054](cfr-054.md)

## Acceptance Criteria

- `doctor --quick` がbranch/opened/root中心に高速実行できる。
- full modeとの差分がJSONで分かる。

## Test Plan

- doctor quick mode test。

## Triage Checklist

- [ ] 再現fixtureまたは現状固定テストを追加した
- [ ] 修正方針が既存ADR/仕様と矛盾しないことを確認した
- [ ] `docs/development/history.md` または関連仕様を更新した
- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` の必要範囲を実行した
