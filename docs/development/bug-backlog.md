# CodeFire Bug Backlog

このファイルは、CodeFire自身を実プロジェクト管理に使いながら見つかった不具合、バグに近いUX、修正候補を管理する。

詳細issueは `docs/development/bug-issue-template.md` に従い、`docs/development/bug-issues/cfb-000.md` の1 issue 1 fileで管理する。このファイルの表は全体サマリとtriage索引として扱う。

## Status

- `open`: 未修正
- `investigating`: 再現条件や修正方針を確認中
- `fixed`: 修正済み
- `wontfix`: 現時点では仕様として扱う

## Bugs

| ID | Status | Area | Summary | Observed while | Impact | Workaround | Candidate fix |
|---|---|---|---|---|---|---|---|
| CFB-001 | fixed | Extinguish / stale resolution | extinguished済みfireのresolutionがstaleになった後、通常CLIで同じfireを再extinguishできない | `algorithm-evolution-agent-lab` で `StaticPlanner` を別moduleへ移動した後の `codefire verify` | `Open fires: 0` でも `Stale resolutions: N` でcommit不能になり、通常操作だけでは復旧しづらい | `codefire extinguish <fire> --refresh ...` で現在のbasisへ更新する | `--refresh` で既存resolutionを `superseded` にし、新しいresolution basisを保存する |
| CFB-002 | fixed | Python Atom extraction | Python methodの派生Atom IDがownerなしの `CODE:path::def:<method>` になり、異なるclassの同名methodでduplicate Atom IDになる | `CodexPlanner.propose` と `StaticPlanner.propose` を同一fileに置いたとき | 正当なPython設計でもduplicate Atom IDでverify/commitが止まる | 明示 `# cf-atom:` でも回避可能 | Python extractorでclass ownerを追跡し、method派生IDを `method:Class.method` にした |
| CFB-003 | fixed | Diagnostics | `verify` は `Missing required links` の件数を表示するが、`commit_policy.require_trace_completeness: false` の場合でも失敗原因に見えやすい | 仕様先行フェーズでtrace completenessを緩和した状態 | 実際のblockerがstale/duplicateでも、missing linkが主原因に見える | `Blocking checks:` 行を見る | verify出力にpolicy上のblockerだけを列挙する `Blocking checks:` を追加した |
| CFB-004 | fixed | Diagnostics | `verify` の失敗件数から対象Atomやstale resolutionを直接確認できない | duplicate Atom IDやmissing linkの原因を調査するとき | active JSONやscan結果を読まないと次の操作対象を特定しづらい | `.codefire-open/state/active_verify.json` を読む | `codefire verify --details` で失敗diagnosticの代表例を表示する |
| CFB-005 | open | UX / Extinguish | fire解消時の `--rationale` / `--evidence` 入力が長く、dogfooding中の人間操作コストが高い | `algorithm-evolution-agent-lab` で複数fireを解消しながらCodeFire commitしたとき | CodeFire処理時間より説明文入力の負担が支配的になり、連続作業の速度を落とす | shell historyや手書きテンプレートを使う | `codefire extinguish --interactive`、複数fire一括解消、直近テスト結果の自動evidence添付を検討する |
| CFB-006 | fixed | UX / Trace completeness | 仕様先行フェーズでmissing required linksが多く、実際のblocker確認時にノイズになる | `algorithm-evolution-agent-lab` で `require_trace_completeness: false` の状態で `verify --details` を使ったとき | open fireは0でもmissing link詳細が大量に出て、次に見るべき問題の優先度が分かりにくい | fixed in global burn-down pass | `verify --details --blocking-only`、filtered JSON data、blocking-only next_actionsでnon-blocking warningを分離した |
| CFB-007 | open | Performance / Observability | `status` / `scan` / `verify` の速度は現規模では軽いが、継続的に測るCLIがない | `algorithm-evolution-agent-lab` 現規模で `status` 約0.4s、`scan` 約0.45s、`verify --details` 約1.2sを手動計測したとき | repo拡大時に遅くなっても、どの処理がボトルネックか追跡しづらい | shellの `time` で手動測定する | `codefire doctor --metrics` または `codefire perf` でAtom数、object数、各phase時間を出す |
| CFB-008 | open | Storage / Artifacts | sealed object storeは小規模では軽いが、ML実験artifactを入れると肥大化し得る | `algorithm-evolution-agent-lab` で `.codefire` 約2.1MB、objects約2.0MB、198 filesを確認したとき | 大きいmetrics/log/model artifactをCodeFire objectに直接入れる運用だとrepositoryが急増する | 重いartifactは外部パスや要約メタデータだけ管理する | artifact retention policy、object store size report、large artifact warning、external artifact reference仕様を追加する |
| CFB-009 | open | Architecture / Maintainability | Rust CLIの機能追加時に `main.rs` へ責務が集まりやすく、module境界が曖昧になり得る | v0.6 Rust rewriteのinstaller/completion作業前レビュー | command dispatch、domain logic、rendering、policyが近接すると不変条件の所在が読みづらくなる | 新規作業前に `module-boundaries.md` を確認し、必要なら先に抽出commitを作る | `main.rs` のdispatch薄型化を継続し、commandごとのplanning/renderingを専用moduleへ分離する |
| CFB-010 | open | UX / Status vs Scan | `status` が `open-consistent` でも、直後の `scan` で多数の `atom_changed` fireが発生する可能性を事前に示さない | `algorithm-evolution-agent-lab` で `status` は `open-consistent` / open fires 0 だったが、`scan` 後に33件のfireが出た | 人間には「今commit可能そう」に見え、scan後に大量fire処理へ進むため作業見積もりがずれる | commit前に必ず `scan` を走らせる | `status --staleness`、`status --predict-scan`、または `status` に `last_scan_base/current_worktree_dirty_atoms` の要約を追加する |
| CFB-011 | open | UX / Extinguish Batch | `verify --details` が出したfire一覧から `extinguish --batch` 用テンプレートを直接生成できない | `algorithm-evolution-agent-lab` で33件のfireを同じテスト証跡で解消したとき | shell loopで `FIRE-001..033` を組み立てる必要があり、ID範囲ミスや証跡入力漏れが起きやすい | shell loopまたは手書きbatch fileを使う | `verify --json` のopen fire一覧から `codefire extinguish --batch-template --evidence-from-command <cmd>` を生成する |
| CFB-012 | fixed | UX / State Label | 全fireをextinguishし `verify --details --blocking-only` が通った後でも、commit前の `status` が `open-burning` / `Open fires: 0` を表示する | `algorithm-evolution-agent-lab` で `MetricNormalizer`、`LeakageChecker`、`NoveltyReviewer` などを消火・verifyした直後 | blockingなしでcommit可能な状態なのに「burning」と読めるため、まだ未処理fireが残っているように見える | fixed in global burn-down pass | verify passed時はscan結果を見て、変更ありなら `open-consistent`、完全cleanなら `open-clean` にする |
| CFB-013 | fixed | UX / Scan Output | clean状態の `scan` が `Changed atoms:` と `Open fires:` の空見出しだけを表示し、明示的に「なし」と言わない | `algorithm-evolution-agent-lab` で `CF-COMMIT-c28f651ba771` 直後に `status` / `verify` / `scan` を実行したとき | 正常にcleanなのか、表示欠落なのかを人間が推測する必要がある | fixed in global burn-down pass | clean scan textは `Changed atoms: none` / `Open fires: none` を表示する |
| CFB-014 | fixed | UX / State Label | `scan` でopen firesが作成された直後でも `status` が `open-consistent` / `Open fires: 4` を表示する場合がある | `algorithm-evolution-agent-lab` で `ConfigurationCapture` / `RunIdentityLedger` 追加後に `scan` で4件fireを作成し、直後に `status` を実行したとき | `open-consistent` という状態名と `Open fires: 4` が矛盾して見え、消火が必要か判断しづらい | fixed in global burn-down pass | `read_status` はopen fire数が正ならraw stateに関わらず `open-burning` を返す |
| CFB-015 | fixed | UX / State Label | clean状態で `verify --details --blocking-only` を実行すると、直後の `status` が `open-clean` から `open-consistent` に変わる | `algorithm-evolution-agent-lab` で `CF-COMMIT-1cf6caf75deb` 後に `status` -> `verify --details --blocking-only` -> `status` -> `scan` -> `status` を実行したとき | worktreeに変更やfireがないのに状態名だけが変わり、clean判定がscan依存に見える | fixed in global burn-down pass | clean verifyは `open-clean` を維持する |
| CFB-016 | fixed | UX / State Label | `scan` でopen firesが作成された直後でも `status` が `open-clean` / `Open fires: 10` を表示する場合がある | `algorithm-evolution-agent-lab` で `LearningCurveAnalyzer` / `CandidatePromotionPolicy` 追加後に `scan` で10件fireを作成し、直後に `status` を実行したとき | `open-clean` は作業不要に見えるが、実際にはfire消火が必要で、状態名と件数の矛盾が非常に強い | fixed in global burn-down pass | open fire数が正なら `status` は `open-clean` を返さない |
| CFB-017 | open | UX / Non-Atom Changes | README、todo、historyなど非Atomドキュメント変更が `scan` の `Changed atoms` に出ず、fire/evidence対象にもならない | `algorithm-evolution-agent-lab` で各機能追加時に `README.md`、`docs/development/todo-checklist.md`、`docs/development/history.md` を更新したが、`scan` はCODE/REQ/TEST Atomだけを表示したとき | ドキュメント変更が封印対象に含まれるのか、証跡不要な変更として扱われるのかが分かりにくく、docs-only変更のレビュー漏れにつながる | 人間が差分を別途確認する | `scan` に `Non-atom changed files` セクションを追加し、必要ならpolicyでfire化/警告化できるようにする |
| CFB-018 | open | UX / Fire Volume | 新規REQを追加すると、REQ->DES、REQ->CODE、REQ->TESTとCODE/TEST->REQの複数fireが同時に発生し、同じ証跡で何度も消火する必要がある | `algorithm-evolution-agent-lab` で `REQ-AUTO-021/022/023/024` や `REQ-SOTA-013/014` を追加したとき、2要求ごとに10件前後のfireが発生した | 実質的に1つの仕様追加レビューなのにedge単位で消火が増幅し、AI/人間の操作量が増える | shell loopで同じevidenceを流す | `fire group` / `trace change set` を導入し、同一REQ追加に伴うedge群をまとめて解消できるようにする |
| CFB-019 | fixed | UX / Fire Identity | `FIRE-001` などのfire IDがscanごとに再利用され、ログや会話で過去のfire参照と衝突しやすい | `algorithm-evolution-agent-lab` で複数の機能追加を繰り返し、毎回 `FIRE-001..010` のようなIDが再利用されたとき | サブエージェントや人間が「FIRE-001」を参照しても、どのscan世代のfireか曖昧になる | v0.7でscan生成fireのdisplay_idをfire key digest由来にし、text出力にstable fire_uidを併記した | CFR-018で修正済み。旧ledgerのfireは従来IDのまま読み取り・指定可能 |
| CFB-020 | open | UX / Extinguish Output | `extinguish` 成功時の出力が `extinguished FIRE-001` だけで、対象AtomやTrace Linkを再表示しない | `algorithm-evolution-agent-lab` で10件単位のfireをshell loopで消火したとき | 消火ログだけを後で見ても、どの要求・実装・テストを解消したか追跡しづらい | 直前の `scan` 出力と照合する | 成功出力に `FIRE-001 CODE-X -> REQ-Y atom_changed` とevidence要約を表示する |
| CFB-021 | open | UX / Commit Summary | `codefire commit` の出力がcommit hash、branch、stateだけで、封印したAtom、fire解消数、証跡数を要約しない | `algorithm-evolution-agent-lab` で `CF-COMMIT-e38475672bec`、`CF-COMMIT-9872184cce2f`、`CF-COMMIT-f825fc13207f` などを作成したとき | 後からログを見ても、そのcommitが何を含んだのかCodeFire出力だけでは分からない | commit messageと直前のscan/verifyログを合わせて読む | commit出力に changed atom count、resolved fire count、non-atom changed files count、messageを表示する |
| CFB-022 | fixed | UX / Verify Output | `verify --details --blocking-only` 成功時の出力が `Verification passed.` だけで、open fire数、stale resolution数、base commitを示さない | `algorithm-evolution-agent-lab` で各消火後にblocking verifyを実行したとき | CIログやサブエージェントログ上で、何が0件だったのか確認しづらく、別途 `status` を実行する必要がある | fixed in global burn-down pass | 成功時も `Open fires: 0`, `Stale resolutions: 0`, `Missing evidence refs: 0`, `Blocking checks: none` などのsummaryを表示する |
| CFB-023 | fixed | UX / Path Options | `status --path . --json`、`scan --path . --json`、`verify --path . --json` が `unsupported ... option: --path` で失敗する | v0.7 install後に `algorithm-evolution-agent-lab/main` を確認し、作業ディレクトリを明示してCodeFireを実行しようとしたとき | CLI利用者やAI agentが多くのコマンドで `--path` を共通指定できると期待しやすいが、実際は `scan [path]` / `verify [path]` の位置引数で、`status` はpath指定自体がないため操作が揺れる | v0.8で `status` / `scan` / `verify` の `--path` / `--path=<path>` を受けるようにした | shared path/json parserとverify parserで `--path` aliasを受ける |
| CFB-024 | fixed | UX / Help | `codefire scan --help` と `codefire verify --help` がサブコマンドhelpではなく `unsupported ... option: --help` を返す | v0.7 install後に `scan` / `verify` の正しいpath指定を確認しようとしたとき | AI agentが使い方を自己修正する導線が弱く、誤ったoptionを試した後に回復しづらい | v0.8で主要local workflow commandとdogfoodingで踏んだcommandのhelpを追加した | `wants_help` / `subcommand_help` によるサブコマンドusage表示 |
| CFB-025 | open | Compatibility / Policy YAML | 旧 `codefire.policy.yaml` の `verification.required` wrapperが `unsupported verification indentation` で拒否され、どのshapeへ直すべきか分かりにくい | v0.7の限定YAML厳格化後、`algorithm-evolution-agent-lab` の既存policyを `verify --json` したとき | 既存CodeFire管理リポジトリがupgrade後にverify不能になり、設定移行の負担が発生する | `verification:` 直下へ `- id: ...` の配列を置く形に手修正する | `migrate check` で旧shapeを検出して修正案を出す、または互換parserで `verification.required` を受ける |
| CFB-026 | open | Automation JSON / Context | `context --changed --json` の主要結果が `data.atoms` / `data.fires` / `data.scan.changed_atoms` に分散し、`scan --json` の `changed_atoms` / `open_fires` と同じ形で読めない | `algorithm-evolution-agent-lab` の `ExperimentRunbookGenerator` 追加中に `scan --json` 後の詳細確認として `context --changed --json` を実行したとき | AI agentがscan結果とcontext結果を同じ処理で扱えず、changed_count/open_fire_countを取り違えやすい | `data.scan.changed_atoms` と `data.fires` を個別に読む | `context_changed` dataに `changed_atoms`, `open_fires`, `changed_count`, `open_fire_count` の互換summaryを追加する |
| CFB-027 | open | UX / Next Actions | `scan --json` のnext_actionsが単発 `context --fire` / `extinguish FIRE-*` を優先し、複数fireがある場合もbatch extinguish templateを提示しない | `ExperimentRunbookGenerator` 追加で4 changed atoms / 6 open firesが出たとき | AI agentが毎回手製のbatch YAMLを作る必要があり、fire数が増えるほど操作が膨らむ | `scan --json` から `jq` でfire IDを抜いてbatch YAMLを作る | next_actionsに `extinguish --batch-template` または `extinguish --batch <generated>` 相当の候補を出す |
| CFB-028 | fixed | Automation JSON / Verify Filter | `verify --details --blocking-only --json` でも `data.missing_required_links` にはnon-blocking warningが全件残る | trace completenessをfalseにしたalgorithm repoでblocking-only verifyを実行したとき | `diagnostics` はblockingだけなのに `data` には警告が残り、AI agentが失敗原因と警告を混同しやすい | fixed in global burn-down pass | JSON dataにfiltered `missing_required_links`、`all_missing_required_links`、`blocking_missing_required_links`、`diagnostic_summary` を追加した |
| CFB-029 | fixed | UX / Next Actions Path | `verify . --json` のnext_actionsが `codefire scan --json` や `codefire context --changed --json` のようにpathなしで返る | algorithm repoのopen dirを明示してverifyしたとき | 別cwdからnext actionを実行するAI agentが、意図しないrepoやopen dirで操作する可能性がある | fixed in global burn-down pass | verify next_actionsのcommandとtargetへ元のpath指定を保持する |
| CFB-030 | fixed | Metrics / Timing | `--metrics` のphase timingで未計測phaseが `0ms` と表示され、実測0msなのか未計測なのか集計しづらい | `scan . --metrics --json` と `verify . --metrics --json` をrunbook開発中に比較したとき | performance監視で0msとして集計され、ボトルネック判断を誤る | v0.8で未計測phaseはJSON timing map上 `null` になった | `MetricPhase.measured=false` のJSON `elapsed_ms` をnull化 |
| CFB-031 | fixed | Metrics / Cache | metricsのcache欄が常に `status=unimplemented` / `disabled_reason=not_implemented` を返し、利用者が調整できる項目に見える | `scan . --metrics --json` と `verify . --metrics --json` を実行したとき | まだ機能していないcache情報が毎回出て、実際に見るべきAtom数・trace数・timingが埋もれる | v0.8でcache詳細blockは未実装時に通常telemetryへ出さず `cache_status=unavailable` とした | cache実装まで詳細cache blockを非表示にする |
| CFB-032 | open | Doctor / Compatibility | `status`、`scan`、`verify`、`commit` は動くのに `doctor . --json` がmissing directoryをblocking errorとしてrepo invalidにする | algorithm repoで `doctor . --quick --json` を実行したとき | 運用可能なrepoが壊れているように見え、AI agentが不要な修復を始める可能性がある | `migrate check --json` も併せて見る | doctorのblocking基準を実運用互換性に合わせ、optional/auto-creatable directoryはwarningに落とす |
| CFB-033 | open | Doctor / Repair UX | doctorが `repairable: true` を出すのにnext_actionsは `codefire doctor --json` の再実行だけで、repair/migrate導線を出さない | missing `.codefire/idempotency` とobject subdirを検出したとき | 人間/AI agentが何を実行すればよいか分からず、同じdoctorを繰り返す | `codefire migrate check --json` を手動で実行する | repairable issueには `migrate dry-run`, `migrate apply`, または `doctor --repair` をnext_actionsに出す |
| CFB-034 | open | Doctor / Migrate Consistency | `doctor` はrepo invalid / blocking 3件、`migrate check` は `compatible: true` / blockers 0件を返し、健康状態の意味が矛盾する | algorithm repoでdoctorとmigrate checkを連続実行したとき | CIやAI agentがどちらをgateにすべきか判断できない | `verify` と `commit` が通るかを最終判断にする | doctor/migrate/verifyで互換性、修復推奨、commit可否の語彙を統一する |
| CFB-035 | open | Migrate / Planned Actions | `migrate check` のplanned actionsがdoctorのmissing directory全件と一致しない場合がある | doctorが `artifact_refs`, `evidence`, `idempotency` をmissingと報告した直後、migrate checkは一部directoryだけをplanned actionに出した | migration planを信じてもdoctorエラーが残る可能性がある | doctorを再実行して差分を確認する | doctor issueとmigrate planned actionを同じlayout definitionから生成する |
| CFB-036 | fixed | UX / Storage Command | top-level helpに `storage` があるが、`storage --json` も `storage . --json` もunsupportedでusageが分からない | algorithm repoの `.codefire` サイズを確認しようとして `storage` を実行したとき | object store肥大化を確認したいdogfooding中に、正しいコマンドを自力発見しづらい | v0.8で `storage [path] [--json]` と `storage report ...` が同じrunnerへ通る | `codefire storage [path] [--json]` のusage/helpとaliasを実装する |
| CFB-037 | fixed | Evidence / Command Failure | `evidence add --from-command ... --json` がcommand exit code 1でも `ok: true` でevidence objectを作成する | runbook test証跡を保存しようとして、コマンドが失敗したのに `CF-EVIDENCE-*` が作られたとき | 失敗コマンドを成功証跡として誤用しやすく、fire解消evidenceの信頼性が落ちる | fixed in global burn-down pass | 非ゼロexit/timeoutは既定で `evidence_command_failed` のJSON failureになり、明示 `--allow-failed-command` 時だけwarningつき `proof_status=failed-command-captured` として保存する |
| CFB-038 | fixed | Evidence / Result Summary | `evidence add --from-command --json` の結果にstdout/stderr要約や保存先object pathがなく、失敗内容をすぐ確認できない | 非ゼロexitのevidence add結果を調べたとき | evidence IDだけでは証跡の中身を判断できず、別途object storeを探す必要がある | v0.8でstdout/stderr summary、truncation、object pathをJSON/text resultへ追加した | result dataにcaptured stdout/stderr summary、truncation、object pathを入れる |
| CFB-039 | fixed | Evidence / Dry Run | 単発 `evidence add --from-command ... --dry-run --json` が `evidence add --dry-run requires --batch` で拒否される | runbook test commandをevidence化する前にdry-run確認しようとしたとき | 証跡追加の副作用を事前確認できず、失敗evidence objectを作りやすい | v0.8で単発evidence add dry-run planを実装した | 単発evidence addにもdry-run planを実装する |
| CFB-040 | fixed | Automation JSON / Show | `show main --json` がJSONではなくtext outputを返す | algorithm repoのbase commit情報を機械的に読もうとしたとき | `jq` がparse errorになり、automationが壊れる | fixed in global burn-down pass | `show --json` をcommand_result envelopeに統一した |
| CFB-041 | fixed | Automation JSON / List | `list --json` が `unsupported option: --json` を返す | algorithm repoのbranch/open一覧を機械的に取得しようとしたとき | AI agentがrepo状態を安全に列挙できない | fixed in global burn-down pass | `list --json` をcommand_result envelopeで実装した |
| CFB-042 | fixed | Automation JSON / Branch | helpに `branch` commandがあるが、`branch --json` は `unsupported branch command: --json` になり、branch一覧・詳細の機械可読導線がない | algorithm repoでbranch状態を確認しようとしたとき | branch管理をAI agentが行うには内部JSON直読みが必要になる | fixed in global burn-down pass | `branch list --json` / `branch show --json` とunsupported branch JSON failureを実装した |
| CFB-043 | open | Automation JSON / Commit Dry Run | `commit -m ... --dry-run --json` がverification blocker時にplain text errorだけを返し、JSON envelopeを返さない | runbook変更のfire解消前にcommit dry-runを実行したとき | automationが失敗理由、open fire数、next actionをJSONで取得できない | 先に `verify --json` を実行する | blocked dry-runでも `ok:false` のcommand_result envelopeを返す |
| CFB-044 | fixed | Automation JSON / Diff | `diff main main --json` はJSONだがcommand_result envelopeではなくraw `codefire_diff` objectを返す | sealed base同士のdiffをautomationで確認したとき | 他コマンドと `ok`, `exit_code`, `schema`, `diagnostics` の扱いが揃わない | fixed in global burn-down pass | diff JSONもcommand_result envelopeで包み、diff payloadを `data` に入れた |
| CFB-045 | open | Diff / Options Consistency | `diff main main --json` の `options.impact` はfalseなのに `impact` objectが出力される | runbook変更前のbase同士をdiffしたとき | option指定とpayload有無が一致せず、AI agentがどの計算が実行されたか判断しづらい | payloadの存在を優先して読む | options flagsと実際に出したsectionsを一致させる、またはdefault included sectionsを明示する |
| CFB-046 | fixed | CLI Help / Init | `codefire init --help` がhelpではなく `--help/.codefire` を作成する | `algorithm-evolution-agent-lab` をCodeFire管理へ戻す前にhelpを確認したとき | help確認が副作用を持ち、作業treeを汚す | fixed in v0.9 Phase A | parser前の `command_help_for_args` で `init --help` をread-onlyにした |
| CFB-047 | fixed | CLI Help / Open | `codefire open --help` がusageではなく `unsupported open option: --help` を返す | 既存algorithm projectのopen/import方法を確認したとき | 重要な復旧/導入commandの使い方をCLIだけで発見できない | fixed in v0.9 Phase A | non-local workflow commandも `command_help_for_args` で共通subcommand helpへ通した |
| CFB-048 | open | Import Workflow | 既存の非空projectをそのままCodeFire open directoryとして採用する標準導線がない | `algorithm-evolution-agent-lab` をCodeFire管理へ戻すとき | 手動move/copy/importが必要で、誤操作や大量fire処理が発生する | 親directoryでinitし、既存projectを退避してopen後にcopy backする | `codefire import` または `init --adopt` を追加する |
| CFB-049 | fixed | Evidence / Command CWD | `evidence add --path . --from-command ...` の既定cwdが期待と違い、open dirのtestsを見つけられない | 初回import証跡としてunit testを保存したとき | 成功するはずの証跡commandが失敗し、失敗evidenceが `ok:true` で保存される | fixed in global burn-down pass | command capture cwd precedenceを explicit `--cwd` then caller cwd と定義し、dry-run/result/evidence payloadにresolved cwd、cwd source、repo-relative cwdを入れる |
| CFB-050 | fixed | Automation JSON / Batch Dry Run | `extinguish --batch --dry-run --json` が全itemのvalidation planを出し、427件で巨大JSONになる | 初回import fire 427件を一括消火する前 | agent/log/context上でdry-run結果が扱いづらい | fixed in global burn-down pass | batch dry-runはdefaultで20件sample + omitted metadataに制限し、`--full`で全件出力できる |
| CFB-051 | open | Automation JSON / Commit Dry Run | `commit --dry-run --json` がchanged Atom 219件を全量で返す | algorithm初回import commit dry-run | dry-run planが大きくなり、意思決定に必要な要約が埋もれる | `jq` で要約する | changed count、sample、omitted metadataをdefaultにする |
| CFB-052 | fixed | UX / Verify Next Actions | clean branchの `verify --json` が変更なしでもcommitをnext_actionに出す | 初回import commit後のclean確認 | AI agentが不要なno-op commitへ進みやすい | fixed in global burn-down pass | verify next_actionsをpending changes awareにした |
| CFB-053 | open | Automation JSON / Scan Size | 初回importの `scan --json` が219 changed atoms / 427 firesを全量で返し巨大になる | algorithm projectを空baseから初回scanしたとき | 大規模repoや初回importでJSONがagent/log上扱いづらい | `jq` でcountsだけ抽出する | scan JSONをbounded defaultにし、full outputは明示optionにする |
| CFB-054 | open | Context / Staleness | `context --changed --json` がfile編集直後でも既存active scanを優先し、古いclean状態を返しうる | iteration planner実装直後、scan前にcontextを確認したとき | agentが変更なしと誤認し、必要なscan/fire対応を飛ばす可能性がある | 必ず `scan --json` を先に実行する | contextがactive scan freshnessを検証し、staleならpreview再計算またはdiagnosticを返す |
| CFB-055 | open | Automation JSON / Scan Shape | `scan --json` の `changed_atoms` がID文字列配列で、詳細objectと思って読むと壊れる | iteration planner実装後にjqでchanged atom sampleを抽出したとき | automationがfield shapeを誤解しやすく、詳細取得導線も分かりづらい | string配列として扱い、詳細はcontext/explainで別取得する | scan JSONに `changed_atom_ids` と `changed_atoms_sample` 等を分ける、またはschema/docsで明確化する |
| CFB-056 | fixed | CLI Help / Batch Schema | `extinguish --help` がbatch schemaを示さず、一般的なtop-level array JSON/YAMLが使えるか判断できない | iteration plannerのfireをbatch extinguishするとき | agentが自然な配列形式を生成して失敗し、正しいwrapper形式をsource/docs検索なしで発見しづらい | fixed in global burn-down pass | helpにminimal batch JSON exampleを追加し、top-level arrayはJSON schema diagnosticでwrapperを案内する |
| CFB-057 | fixed | Automation JSON / Show | `show main --json` がexit 0でhuman textを返す | risk register dogfooding前のsealed commit確認 | automationがJSON parseに失敗し、成功/失敗判定を誤る | fixed in global burn-down pass | show JSONをcommand_result envelopeで返す |
| CFB-058 | fixed | Branch / JSON Failure | `branch show --json` がplain text unsupported errorを返す | branch state確認surfaceを探したとき | branch詳細取得のautomation導線がなく、失敗もJSONで扱えない | fixed in global burn-down pass | `branch show --json` を実装し、unsupported branch subcommandもJSON envelope化した |
| CFB-059 | fixed | Batch / JSON Failure | batch系commandで存在しないbatch fileを指定すると `--json` でもplain text OS errorになる | evidence/link/fire/extinguish batchの失敗面を確認したとき | batch automationがmissing fileを機械判定できない | fixed in global burn-down pass | batch file read failureを `batch_file_missing` JSON diagnosticsとnext_actionsへ包む |
| CFB-060 | fixed | Context/Explain / JSON Failure | `context --atom <missing> --json` と `explain atom <missing> --json` がplain text errorになる | risk register dogfooding前にAtom detail取得の失敗面を確認したとき | agentがunknown Atomとparse failureを区別できない | fixed in global burn-down pass | unknown selector errorsをJSON envelopeで返し、refresh_scan next_actionを返す |
| CFB-061 | fixed | Storage / Remote Diagnostics | `storage . --remote <missing> --json` が `ok:true` かつdiagnosticsなしでremote情報を落とす | missing remote storage確認をしたとき | remote容量確認が成功に見え、監査漏れになる | fixed in global burn-down pass | requested remoteは常に `remotes[]` に出し、missing/layout不足は `status=invalid_layout`、`missing_required_paths`、`remote_layout_invalid` warningで返す |
| CFB-062 | fixed | Diff / JSON Failure | `diff main <missing> --json` がplain text unknown branch errorになる | diff失敗面を確認したとき | automationが比較対象解決失敗をJSONで処理できない | fixed in global burn-down pass | diff target resolution failuresをJSON envelopeで返し、branch list next_actionを追加した |
| CFB-063 | open | Review/Patch / JSON Surface | `review-pack main --json` と `patch export main --json` がunsupported optionのplain text errorになる | review/patch automation surfaceを確認したとき | review artifact生成やpatch exportを機械制御しづらい | text/file outputだけを使う | review-pack/patch exportのJSON plan/resultまたはstructured unsupported diagnosticsを実装する |
| CFB-064 | fixed | Remote / JSON Surface | `list <remote> --json` と `request-list <remote> --json` がunsupported optionのplain text errorになる | remote list失敗面を確認したとき | remote state確認をautomationで標準化できない | fixed in global burn-down pass | remote read commandsのJSON envelopeを実装した |
| CFB-065 | fixed | Remote / Dry Run Contract | `upload --dry-run --json` のtop-level `repo` がnullで、plan内next_actionsがtop-level next_actionsへ出ない | missing remote upload planを確認したとき | automationがrepository targetや次操作をenvelope共通fieldから読めない | fixed in global burn-down pass | dry-run envelopeのrepo/next_actionsをplan内容と整合させた |
| CFB-066 | open | Install / PATH precedence | `install.sh --prefix ~/.cargo` は成功したが、PATH上の有効 `codefire` は古い `~/.local/bin/codefire` のままだった | v0.9 Phase Aのhelp修正をinstallしてsmokeしたとき | 修正済みbinaryを使っているつもりで古いbinaryを実行し、`init --help` の副作用を再発させた | `which codefire` を確認し、PATH先頭のprefixへinstallし直す | install後にactive `codefire` path/version/help fingerprintを確認し、prefixがPATH先頭でない場合はwarningを出す |
| CFB-067 | open | Commit / JSON Contract | `commit --json` 成功時のtop-level `repo` がnullで、実行済みなのにplan内next_actionsがapply commitを示す | evidence pack builderをCodeFire commitしたとき | automationがcommit後のrepo/branch/next actionを共通fieldから読めず、apply済み操作を未適用planのように扱う可能性がある | `data.plan.open_dir` と `data.plan.branch` を読む | commit successはtyped result dataを返し、top-level repoを埋め、apply済みplan next_actionを出さない |
| CFB-068 | fixed | Automation JSON / Next Actions Path | `scan --path . --json` のnext_actionsが `codefire context --changed --json` や `codefire extinguish ...` のようにpathなしで返る | claim review queue追加後にalgorithm open directoryから `scan --path . --json --metrics` を実行したとき | agentがnext_actionsを別cwdや親processから実行すると、意図しないrepo/open directoryを参照する可能性がある | fixed in global burn-down pass | scan next_actionsのcommandとtargetへ元のpath指定を保持する |
| CFB-069 | fixed | Automation JSON / Verify Filter | `verify --blocking-only --json` のnext_actionsが、非blockingのmissing required linksに対するcontext actionを大量に返す | `trace_completeness_required=false` のalgorithm repoでopen fireがある状態の `verify --path . --details --blocking-only --json --metrics` | blocking-onlyを使うautomationが、実際のblocking原因であるopen fireより非blocking link warningsへ誘導されやすい | fixed in global burn-down pass | `--blocking-only` 時のnext_actionsはblocking diagnosticsだけから生成する |
| CFB-070 | fixed | Automation JSON / Operation Plan | `extinguish --dry-run --json` のtop-level `repo` がnullで、plan内next_actionsがtop-levelへ出ない | claim audit trail追加後に1 fireのdry-run extinguishを確認したとき | dry-run planを扱うautomationがrepoと次操作を共通envelopeから読めず、command-specific parsingが必要になる | fixed in global burn-down pass | local operation plan envelopeのtop-level `repo` と `next_actions` をplan内容から埋めた |
| CFB-071 | fixed | CLI Path Contract | `storage --path ... --json`、`doctor --path ... --json`、`migrate check --path ... --json` がunsupported optionのplain text errorになる | claim audit trail dogfoodingでrepo health/storage確認を別cwdから実行したとき | 多くのcommandで `--path` が使えるためAI agentが共通指定を期待するが、health/storage/migrateで失敗しJSON recoveryもできない | fixed in global burn-down pass | storage/doctor/migrateが`--path`/`--path=`を受け、JSON parser failureもenvelopeで返す |
| CFB-072 | fixed | Evidence / Dry Run CWD | `evidence add --path . --from-command ... --dry-run --json` のplanで `cwd:null` になり、実際にどこでcommand captureされるか分からない | claim audit trail dogfoodingでtest evidence追加前に単発dry-runを確認したとき | evidence dry-runを見ても、テストがopen dirで実行されるのか呼び出しcwdで実行されるのか判断できない | fixed in global burn-down pass | evidence dry-run planに `requested_cwd`、`resolved_cwd`、`cwd_source`、`repo_relative_cwd`、`allow_failed_command` を入れる |
| CFB-073 | fixed | Context / Bounded Output | `context --changed --limit 2 --json` はtruncated flagsだけを返し、総件数や省略件数を返さない | claim audit trail dogfoodingでchanged contextを小さいlimitで確認したとき | automationが「2件だけ存在する」のか「2件だけ表示された」のかをcountsなしで判断しづらい | fixed in global burn-down pass | context JSONにsummary countsとcontext_expand next_actionを追加した |
| CFB-074 | fixed | Automation JSON / Next Action Schema | `explain fire --json` のnext_actionsが `id/description/context` 形式で、scan/status/verifyの `kind/reason/target` 形式と違う | claim audit trail dogfoodingで `explain fire FIRE-* --path . --json` を実行したとき | 共通next_actions executorがcommandごとのfield差を吸収する必要がある | fixed in global burn-down pass | next_actions schemaを全commandで統一し、旧fieldは互換aliasとして残す |
| CFB-075 | fixed | Automation JSON / Bounded Next Actions | `scan --json` の `next_actions_omitted` が省略actionの種類や復元commandを示さず、`codefire status --json` を薦める | claim release gate dogfoodingで6 fireのscan next_actionsが12件上限に達したとき | 省略されたfire-specific context/extinguish actionsをどう取得すべきか分からず、statusを実行しても同じaction集合は戻らない | fixed in global burn-down pass | omitted next_actionsにomitted_by_kind、first_omitted_targets、復元commandを入れる |
| CFB-076 | fixed | Batch / Dry Run Validation | `link --batch --dry-run --json` が `valid:false` かつerror diagnosticsでもtop-level `ok:true` / exit 0になる | clean状態で既存linkを含むbatchをdry-runしたとき | automationがdry-run validation失敗を成功扱いし、apply可能と誤解しやすい | fixed in global burn-down pass | invalid link batch dry-runは `ok:false` / exit 2 / diagnostics付きJSON envelopeを返す |
| CFB-077 | open | Fire Identity | manual `fire --dry-run` / apply結果が `FIRE-001` と `fire_<12hex>` 形式を使い、scan fireのstable display/uid形式と揃わない | clean状態でmanual fire dry-run/applyを試したとき | fire IDの扱いがscan由来とmanual由来で分かれ、ログ参照やbatch生成が混乱する | display_idとfire_uidの両方を記録する | manual fireもscan fireと同じstable display_id/fire_uid形式へ寄せる |
| CFB-078 | open | Manual Fire / State Recovery | manual fireを作成・解消すると、source changeなしでも `open-consistent` になりcommit next_actionが出る | manual fire apply probe後にobsolete resolutionで解消したとき | 調査用fireや誤登録fireの解消だけでno-op commitへ誘導される | `scan --path . --json` を実行してopen-cleanへ戻す | source changesなし・open firesなしの場合はscan不要でcleanへ戻すか、discard active manual fire/resolution導線を出す |
| CFB-079 | fixed | CLI Help / Trace Graph | `codefire trace-graph --help` がusageではなくtrace graph JSONを出す | link batch dry-run next_actionの `trace-graph` を確認したとき | help確認がdata commandとして実行され、正しいoptionやJSON契約が分からない | fixed in v1.0 Phase 1a | `command_help_for_args` にdebug/read-only command help routingを追加した |
| CFB-080 | fixed | Trace Graph / JSON Contract | `codefire trace-graph --path . --json` がcommand_result envelopeではないraw JSONを返し、linksも空になる | link batch dry-run next_actionをalgorithm open dirで実行したとき | Trace Linkが多数あるrepoで空graphに見え、automationがtrace欠落と誤判断する | fixed in next batch | trace-graph commandを正式CLI契約化し、path/jsonを解釈し、envelopeで実Trace Graphを返す |
| CFB-081 | fixed | Explain / Verification | `explain verify-failure --json` がpassed verificationのnon-blocking warningsを `blocker(s)` と要約する | clean algorithm projectでverification説明を確認したとき | AI agentがcommit可能な状態をblocking failureと誤認しやすい | fixed in global burn-down pass | explain summaryをblocking diagnosticsに合わせ、passed状態はwarningsとして要約する |
| CFB-082 | fixed | Storage / Quick Report | `storage --quick --json` のlargest object typeが、IDやpathから推定できるのに `unscanned` になる | algorithm projectの記憶領域影響をquick確認したとき | quick storageでどのobject familyが肥大化しているか分からない | fixed in global burn-down pass | quick modeでもID prefix/subdirからlargest object typeを推定し、`type_confidence` と `coverage` を返す |
| CFB-083 | fixed | Branch / Metrics | `branch list --json --metrics` が成功するがmetrics blockを返さずdiagnosticも出ない | branch stateをmetrics付きで確認したとき | common flagを投げるautomationが、metrics未対応と空metricsを区別できない | fixed in global burn-down pass | `branch list` へmetrics block、branch counters、help記載を追加した |
| CFB-084 | fixed | Migrate / JSON Failure | `migrate check <path> --json --metrics` がplain text `unsupported migrate option` を返す | migrate compatibilityを共通observability flag付きで確認したとき | health/recovery commandの失敗をautomationが構造化処理できない | fixed in global burn-down pass | migrate parser errorをJSON failure envelopeへ包む |
| CFB-085 | fixed | CLI Help / Recovery | `migrate --help`、`context --help`、`explain --help` がusageではなくunsupported errorになる | command discovery surfaceを確認したとき | AI agentが失敗後にhelpで自己修正できない | fixed in v1.0 Phase 1a | shared help routingをmigrate/context/explainへ拡張した |
| CFB-086 | fixed | Extinguish / Evidence JSON | `extinguish --evidence-ref ... --json` が `evidence_refs` を返す一方で `has_evidence:false` を返す | algorithm cycle 3でResearchFrontierMapのfireを証跡付き解消したとき | automationが証跡付きresolutionを証跡なしと誤判定し得る | fixed in next cycle | `has_evidence` をinline evidenceまたはevidence refsの存在へ揃えた |
| CFB-087 | fixed | Extinguish / JSON Failure | `extinguish --evidence-ref <missing> --dry-run --json` がplain text `object not found` を返す | algorithm cycle 3でFrontierExperimentPlannerのfire解消preflightをしたとき | dry-run automationがmissing evidenceを構造化診断できず、証跡作成へ復旧しづらい | fixed in active batch | missing evidence-ref lookup failureを `ok:false` JSON envelopeへ包んだ |
| CFB-088 | fixed | Evidence / Dry Run JSON | `evidence add --dry-run --json` が実体のない `evidence_id:""` を返す | FrontierExperimentPlanner commit後のclean-state probeでevidence preflightを確認したとき | automationが空文字IDを実IDとして扱う可能性がある | fixed in next cycle | dry-runでは `evidence_id:null` と `created:false` を返し、実作成時は `created:true` を返す |
| CFB-089 | fixed | Debug Commands / JSON Contract | `atom-index --path . --json` と `missing-links --path . --json` が `--path` をpathとして扱いplain OS errorになる | clean-state probeでdebug/read-only surfaceを確認したとき | AI toolがcore trace/debug dataを共通path/json指定で取得できない | fixed in active batch | atom-index/missing-linksを通常command化し、shared path/json parserとenvelopeを使う |
| CFB-090 | fixed | Context / Bounded Graph | `context --changed --limit 5 --json` が省略されたAtomへ向かう `trace_links` を返し、dangling参照か省略済み参照かを区別できない | FrontierFeedbackIntegrator dogfoodingでchanged contextをbounded取得したとき | graph consumerが返却済みAtom集合だけでTrace Graphを構築すると欠落node参照になり、追加fetch判断もできない | fixed in active batch | bounded contextはlink endpointのreturned/omitted状態、omitted endpoint count、再取得commandを返す |
| CFB-091 | fixed | Atom Extraction / Explicit Atom Span | 末尾側の明示 `cf-atom` の後ろに新しい明示Atomを追加すると、既存Atomのcontent hashが変わり余分なfireが出る | FrontierDriftReporter追加時に既存FrontierFeedbackIntegrator Atomと既存feedback test Atomにもfireが出たとき | 新機能追加だけで隣接する既存Atomの再証跡が必要になり、docs-first開発のfire数が膨らむ | fixed in next cycle | Python explicit Atomはmarker直後のclass/def/async def/decorated symbolのindent blockをhash範囲にし、後続Atom追加で前Atomが縮む挙動を避ける |
| CFB-092 | fixed | Atom Extraction / Migration | 明示Atom抽出アルゴリズムを改善しただけで、対象プロジェクトのsourceが無変更でも大量の `atom_changed` fireが出る | CFB-091修正版をinstallし、`algorithm-evolution-agent-lab` をscanしたとき | 抽出ルール改善やhash仕様変更が、実装変更ではなくtool migrationであることをCodeFireが区別できず、100件超の消火と証跡処理が必要になる | fixed in active batch | atom hash schema versionとsource-unchanged検出によりtool migrationとして報告し、rebaseline commitで封印できるようにした |

## Triage Notes

- CFB-001は `test_stale_resolution_blocks_verify` でrefresh導線を確認する。
- CFB-002は `test_python_methods_include_class_owner_in_derived_atom_id` で同名methodの派生ID衝突回避を確認する。
- CFB-003は既存verify失敗系テストで `Blocking checks:` を確認する。
- CFB-004は `test_verify_details_reports_missing_required_links` とduplicate Atom ID詳細表示で確認する。
- CFB-005からCFB-008は、`algorithm-evolution-agent-lab` dogfoodingでの使いやすさ、速度、記憶領域観察から登録した改善issue。
- CFB-009は、v0.6以降のRust本流で単一ファイル肥大化を防ぐための開発プロセス改善issue。
- CFB-010とCFB-011は、v0.6 Rust defaultをインストールした後の `algorithm-evolution-agent-lab` Phase 4作業で再確認したdogfooding issue。
- CFB-012は、連続して新Atomを追加し、各fireを消火してからcommitする運用で再現した。`open-burning` は内部的には「未sealed変更あり」を含む可能性があるが、fire数0のときは人間には未消火に見えやすい。
- CFB-013は、clean branchの確認時に再現した。成功状態の出力ほど明示的なempty stateを持つ方が、サブエージェント運用時のログ解釈も安定する。
- CFB-014は、CFB-012とは逆向きの状態ラベル不整合として記録した。fire数が正ならラベルも未消火状態を示すべきである。
- CFB-015は、`verify` 自体が成功しているにもかかわらず状態名が変化する問題として記録した。`scan` 後には `open-clean` に戻るため、状態算出の入力がコマンドごとに揺れている可能性がある。
- CFB-016は、CFB-014より強い矛盾として記録した。`open-clean` とopen fire数正の組み合わせは、状態名の意味を壊すためblocking相当のUX不具合として扱う。
- CFB-017は、Atom中心モデル自体は維持しつつ、Atom外の実ファイル差分も人間が見落とさないようにするためのdogfooding改善issueである。
- CFB-018からCFB-022は、同じ操作を繰り返したことで見えたAI運用向けのログ・ID・集約性改善issueである。特にfire grouping、安定ID、成功時summaryはサブエージェントにCodeFireを使わせる場合の誤操作を減らす。
- CFB-005の具体例として、同一evidenceで多数fireを解消する場合は `verify` からbatch templateを生成できると操作量が大きく減る。
- CFB-006の具体例として、`require_trace_completeness: false` でもmissing link件数が表示されるため、`--blocking-only` とnon-blocking診断の優先度分離は引き続き重要である。
- CFB-023からCFB-025は、v0.7をinstallしてalgorithm projectへ戻した直後の再開手順で踏んだ。共通path option、subcommand help、policy migrationの3つはAI agentが自力復旧するうえで優先度が高い。
- CFB-017は、`algorithm-evolution-agent-lab` のドキュメント中心開発への再整備でも再現した。`REQ-PROCESS-*` / `DES-PROCESS-*` は `scan` のchanged atomsへ出るが、同時に更新した `AGENTS.md`、`README.md`、`docs/development/implementation-roadmap.md`、`docs/development/todo-checklist.md`、`docs/development/history.md` はscan要約に出ないため、ドキュメント中心運用ほどnon-atom changed filesの明示が重要になる。
- CFB-026からCFB-045は、`algorithm-evolution-agent-lab` で `ExperimentRunbookGenerator` をdocs-firstで追加しながら、context、verify metrics、doctor/migrate、storage、evidence、show/list/branch、commit dry-run、diffを実際に叩いて登録した。主な傾向はJSON envelope不統一、next_actionsのpath/batch不足、doctor/migrateの健康状態不一致、evidence command failureの扱いである。
- CFB-046からCFB-053は、v0.8 first implementation passをinstallし直し、`algorithm-evolution-agent-lab` をCodeFire管理へ戻して初回import commitを作る過程で見つけた。主な傾向はhelpの副作用、既存project import導線不足、大量JSONのboundedness不足、evidence command cwd、clean state next_actionsである。
- CFB-054からCFB-055は、`ExperimentIterationPlanner` をdocs-firstで追加し、scan前後のcontextとscan JSONを叩いたときに見つけた。主な傾向はactive scan freshnessとJSON field shapeの読み取りやすさである。
- CFB-056は同じiteration planner変更のfire消火時に見つけた。既存batch schemaは使えるが、helpとerrorだけでは正しいwrapper構造へ到達しづらい。
- CFB-057からCFB-065は、`ExperimentRiskRegister` を追加する前後にshow、branch、batch失敗、context/explain失敗、storage remote、diff失敗、review/patch、remote list/upload dry-runを叩いて見つけた。これで現在cycleのdogfood issueはCFB-046..CFB-065の20件に到達した。
- CFB-066からCFB-080は、cycle 2のalgorithm dogfoodingとclean-state probeで見つけた。主な傾向はinstall PATH、commit/dry-run envelope、next_actions path/filter/schema、health/storage path contract、context bounded output、manual fire identity/state recovery、trace-graph help/path/jsonである。
- CFB-081からCFB-085は、cycle 2 dogfood issue目標20件へ到達するため、clean状態でexplain/storage/branch/migrate/context help surfaceを調査して追加した。主な傾向は、passed状態の要約不整合、quick reportの情報欠落、metrics flagの扱い、JSON failure envelope、help routingである。
- CFB-086以降は、cycle 3のalgorithm dogfoodingで見つかったissueとして追加する。主な傾向は、v1.0で入れたhelp改善後に残るoperation JSON/evidence/next_actions/state freshnessである。
